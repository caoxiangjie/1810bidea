<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk.xys.dao.LoginDao">
    <select id="queryPhoneNumber" parameterType="string" resultType="com.jk.xys.pojo.LoginBean">
        select * from t_loginuser where  name =#{value}
    </select>
</mapper>